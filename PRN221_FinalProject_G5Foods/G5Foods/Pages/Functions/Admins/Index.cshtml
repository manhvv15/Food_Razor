@* @page
@model G5Foods.Pages.Functions.Admins.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Admin[0].AdminName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Admin[0].AdminEmail)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Admin[0].AdminPassword)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Admin) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AdminName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AdminEmail)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AdminPassword)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.AdminId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.AdminId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.AdminId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
 *@